The data folder contains the output of experiments and model simulations, 
which will be used as input to analyses.

raw data...data that is stored as collected
clean data...data cleaned to use for analyses


